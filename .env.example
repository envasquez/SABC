# SABC Environment Variables Template
#
# SECURITY WARNING: Never commit .env files with real secrets!
# Copy this file to .env and replace ALL placeholder values with real secrets.
#
# To generate secure secrets, run:
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"

# ============================================================================
# CRITICAL SECURITY SETTINGS - Must be changed for production!
# ============================================================================

# Application Secret Key (64+ characters recommended)
# Used for session encryption, CSRF tokens, password reset tokens
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_64_CHARACTER_STRING_MINIMUM

# Database Connection
# Local development: postgresql://postgres:dev123@localhost:5432/sabc
# Production: Use managed database URL from hosting provider
DATABASE_URL=postgresql://postgres:CHANGE_PASSWORD@localhost:5432/sabc

# Alternative: Individual DB components (not recommended)
# DB_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# ============================================================================
# Email Configuration (Required for password reset)
# ============================================================================

# Gmail SMTP Settings
# IMPORTANT: Use Gmail App Password, NOT regular password
# Setup: https://myaccount.google.com/apppasswords
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=CHANGE_THIS_TO_GMAIL_APP_PASSWORD
FROM_EMAIL=your-email@gmail.com

# Base URL for email links
WEBSITE_URL=http://localhost:8000

# ============================================================================
# Application Configuration
# ============================================================================

# Environment mode: development, staging, production
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Debug mode (MUST be false in production)
DEBUG=true

# Application port
PORT=8000

# ============================================================================
# Monitoring & Observability (Optional)
# ============================================================================

# Sentry Error Monitoring (https://sentry.io)
# Leave blank to disable error tracking
# Get your DSN from: https://sentry.io/settings/projects/sabc/keys/
# SENTRY_DSN=https://your-key@o12345.ingest.sentry.io/67890

# Release version for deployment tracking (optional)
# RELEASE_VERSION=v1.0.0

# ============================================================================
# Optional: Advanced Settings
# ============================================================================

# Custom ports (uncomment if needed)
# HTTP_PORT=80
# HTTPS_PORT=443

# Session timeout (seconds, default 86400 = 24 hours)
# SESSION_MAX_AGE=86400

# Rate limiting (requests per time window)
# RATE_LIMIT_LOGIN=5/minute
# RATE_LIMIT_REGISTER=3/hour

# ============================================================================
# Security Checklist for Production
# ============================================================================
# [ ] SECRET_KEY is random and at least 64 characters
# [ ] DATABASE_URL uses strong password (20+ random characters)
# [ ] SMTP_PASSWORD is Gmail App Password (16 chars)
# [ ] ENVIRONMENT=production
# [ ] DEBUG=false
# [ ] WEBSITE_URL is your production domain with HTTPS
# [ ] All passwords are unique (not reused from dev/staging)
# [ ] This .env file is NOT committed to git
# [ ] Credentials are stored in platform secrets (Digital Ocean/AWS/etc)
# ============================================================================
