version: '3.9'

services:
  db:
    image: postgres
    volumes:
    - ./data/db:/var/lib/postgresql/data
    env_file:
      - db_vars.env
  sabc:
    env_file:
      - django_vars.env
    build:
      context: .
    container_name: sabc
    command: >
            sh -c "
            python sabc/manage.py makemigrations
            && python sabc/manage.py migrate --run-syncdb --noinput
            && python sabc/manage.py createsuperuser --noinput
            && python sabc/manage.py runserver 0.0.0.0:8000
            "
    restart: unless-stopped
    volumes:
      - sabc-app:/app
    ports:
      - 8000:8000
    depends_on:
      - db
volumes:
  sabc-app: